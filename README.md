# RAG_Mongodb
A complete RAG pipeline implementation in Node.js. Converts blog content into semantic vectors via Voyage AI, stores them natively in MongoDB Atlas, and utilizes $vectorSearch alongside AI Reranking (rerank-2) to provide high-precision contextual search results beyond standard keyword matching.
